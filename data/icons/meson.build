images = [
   ['loongson-perf.png'],
   ['loongson-state.png'],
   ['loongson-mb.png'],
   ['loongson-spec.png'],
   ['loongson-security.png']
  ]
foreach image: images
  install_data(image,
    install_dir : join_paths(get_option('prefix'), 'share', 'loongson-info', 'icons')
  )
endforeach
